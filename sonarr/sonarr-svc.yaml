---

apiVersion: v1
kind: Service
metadata:
  name: sonarr-svc
spec:
  selector:
    app: sonarr
  type: NodePort
  ports:
    - protocol: TCP
      port: 8989
      targetPort: 8989
      nodePort: 30989
